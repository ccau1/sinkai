version: "3.9"

services:
  app:
    image: ${APP_IMAGE}
    ports:
      - "5280:80"
      - "52443:443"
    volumes:
      - ./nginx.ssl.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    restart: unless-stopped
